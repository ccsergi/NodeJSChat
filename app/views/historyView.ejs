<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Historial de Chat</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        #historialRecuadro {
            max-height: 500px;

            overflow-y: auto;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 10px;
        }

        .list-group-item {
            border: none;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/chat/history/list">Historial</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/chat/list">Chat</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h2>Historial de Chat</h2>

                <% if (messages.length> 0) { %>
                    <div id="historialRecuadro" class="list-group">
                        <% messages.forEach(function(message) { %>
                            <div class="list-group-item">
                                <strong>
                                    <%= message.user %>
                                </strong> - <%= message.timestamp %>
                                    <p class="mb-0">
                                        <%= message.message %>
                                    </p>
                            </div>
                            <% }); %>
                    </div>

                    <!-- Verifica si hay mensajes antes de mostrar el formulario -->
                    <% if (messages.length> 0) { %>
                        <form id="deleteForm" action="/chat/history/delete/<%=sala%>" method="get">
                            <button type="submit" class="btn btn-danger mt-3">Eliminar Historial</button>
                        </form>
                        <% } %>

                            <% } else { %>
                                <p>No hay mensajes en esta sala.</p>
                                <% } %>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>

</html>